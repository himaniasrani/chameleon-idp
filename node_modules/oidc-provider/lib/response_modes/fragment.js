const formatUri = require('../helpers/redirect_uri');
const open = require('open');

module.exports = (ctx, redirectUri, payload) => {
  const uri = formatUri(redirectUri, payload, 'fragment');
  ctx.status = 200;
  console.log("fragment.js")
  // ctx.redirect(uri);
  ctx.type = "json";
  ctx.body = {
    "uri": uri
  }
};
